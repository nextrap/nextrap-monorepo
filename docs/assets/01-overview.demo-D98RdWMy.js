import './_virtual_tdemo-client-Pi1VR-d9.js';
import './index-l0sNRNKZ.js';
import './main-BAN3CDRl.js';
import { d as n } from './types-4rIte7rE.js';
const e = `# NTE Spinner

\`<nte-spinner>\` displays an indeterminate loading animation by default. State classes switch the same element to determinate progress or a final status.

## Loading

<div class="nte-spinner-demo-grid">
  <figure>
    <nte-spinner></nte-spinner>
    <figcaption>Loading</figcaption>
  </figure>
</div>

## Progress and status states

<div class="nte-spinner-demo-grid">
  <figure>
    <nte-spinner class="progress" style="--percentage: 30; --percentage-txt: '30%'"></nte-spinner>
    <figcaption><code>progress</code></figcaption>
  </figure>
  <figure>
    <nte-spinner class="checked"></nte-spinner>
    <figcaption><code>checked</code></figcaption>
  </figure>
  <figure>
    <nte-spinner class="cross"></nte-spinner>
    <figcaption><code>cross</code></figcaption>
  </figure>
  <figure>
    <nte-spinner class="info"></nte-spinner>
    <figcaption><code>info</code></figcaption>
  </figure>
  <figure>
    <nte-spinner class="warning"></nte-spinner>
    <figcaption><code>warning</code></figcaption>
  </figure>
</div>

Progress is controlled with \`--percentage\` (0–100) and \`--percentage-txt\`. Animation speed and colors can be customized with the documented CSS custom properties.
`,
  o = n({ title: 'Overview', description: 'Loading, determinate progress and all status variants', markdown: e });
export { o as default };
