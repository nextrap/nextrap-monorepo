import { expect } from 'vitest';
import { NtlCardRowElement } from './ntl-card-row';

describe('ntl-card-row', () => {
  it('should create an element', () => {
    const el = new NtlCardRowElement();
    expect(el).toBeInstanceOf(NtlCardRowElement);
  });

  it.todo('write some real tests!');
});
