import { expect } from 'vitest';
import { NtlCardRow } from './ntl-card-row';

describe('ntl-card-row', () => {
  it('should create an element', () => {
    const el = new NtlCardRow();
    expect(el).toBeInstanceOf(NtlCardRow);
  });

  it.todo('write some real tests!');
});
