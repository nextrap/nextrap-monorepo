/**
 * Shared utility functions for file operations.
 */

/*
export function collectFormData(parentElement : HTMLElement): Record<string, FormDataEntryValue> {
  let formData = new FormData();
  parentElement.querySelectorAll('input, select, textarea, [name]').forEach((element) => {
      const name = (element as HTMLInputElement).name ??  element.id ?? null;
      if ( ! name) {
        return;
      }



  })
};
*/
