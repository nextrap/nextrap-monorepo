import { expect } from 'vitest';
import { NtlCardElement } from './ntl-card';

describe('ntl-card', () => {
  it('should create an element', () => {
    const el = new NtlCardElement();
    expect(el).toBeInstanceOf(NtlCardElement);
  });

  it.todo('write some real tests!');
});
