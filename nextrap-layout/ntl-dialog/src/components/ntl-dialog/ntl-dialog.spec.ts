import { expect } from 'vitest';
import { NtlDialogElement } from './ntl-dialog';

describe('ntl-dialog', () => {
  it('should create an element', () => {
    const el = new NtlDialogElement();
    expect(el).toBeInstanceOf(NtlDialogElement);
  });

  it.todo('write some real tests!');
});
