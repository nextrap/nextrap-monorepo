import { expect } from 'vitest';
import { NtlAccordionElement } from './ntl-accordion';

describe('ntl-accordion', () => {
  it('should create an element', () => {
    const el = new NtlAccordionElement();
    expect(el).toBeInstanceOf(NtlAccordionElement);
  });

  it.todo('write some real tests!');
});
