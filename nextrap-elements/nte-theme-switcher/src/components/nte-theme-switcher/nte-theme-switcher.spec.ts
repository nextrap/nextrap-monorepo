import { expect } from 'vitest';
import { NteThemeSwitcherElement } from './nte-theme-switcher';

describe('nte-theme-switcher', () => {
  it('should create an element', () => {
    const el = new NteThemeSwitcherElement();
    expect(el).toBeInstanceOf(NteThemeSwitcherElement);
  });

  it.todo('write some real tests!');
});
