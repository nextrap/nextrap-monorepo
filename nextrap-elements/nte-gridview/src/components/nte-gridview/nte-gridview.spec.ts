import { expect } from 'vitest';
import { NteGridviewElement } from './nte-gridview';

describe('nte-gridview', () => {
  it('should create an element', () => {
    const el = new NteGridviewElement();
    expect(el).toBeInstanceOf(NteGridviewElement);
  });

  it.todo('write some real tests!');
});
