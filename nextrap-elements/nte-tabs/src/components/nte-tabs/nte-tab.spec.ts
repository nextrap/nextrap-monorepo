import { expect } from 'vitest';
import { NteTabsElement } from './nte-tabs';

describe('nte-tabs', () => {
  it('should create an element', () => {
    const el = new NteTabsElement();
    expect(el).toBeInstanceOf(NteTabsElement);
  });

  it.todo('write some real tests!');
});
