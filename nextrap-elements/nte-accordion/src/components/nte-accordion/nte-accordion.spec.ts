import { expect } from 'vitest';
import { NteAccordionElement } from './nte-accordion';

describe('nte-accordion', () => {
  it('should create an element', () => {
    const el = new NteAccordionElement();
    expect(el).toBeInstanceOf(NteAccordionElement);
  });

  it.todo('write some real tests!');
});
