import { expect } from 'vitest';
import { NtlCardGridElement } from './ntl-card-grid';

describe('ntl-card-grid', () => {
  it('should create an element', () => {
    const el = new NtlCardGridElement();
    expect(el).toBeInstanceOf(NtlCardGridElement);
  });

  it.todo('write some real tests!');
});
