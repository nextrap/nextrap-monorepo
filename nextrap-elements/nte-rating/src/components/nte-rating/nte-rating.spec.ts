import { expect } from 'vitest';
import { NteRatingElement } from './nte-rating';

describe('nte-rating', () => {
  it('should create an element', () => {
    const el = new NteRatingElement();
    expect(el).toBeInstanceOf(NteRatingElement);
  });

  it.todo('write some real tests!');
});
