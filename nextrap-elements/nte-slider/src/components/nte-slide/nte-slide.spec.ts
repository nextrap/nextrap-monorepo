import { expect } from 'vitest';
import { NteSlideElement } from './nte-slide';

describe('nte-slide', () => {
  it('should create an element', () => {
    const el = new NteSlideElement();
    expect(el).toBeInstanceOf(NteSlideElement);
  });

  it.todo('write some real tests!');
});
