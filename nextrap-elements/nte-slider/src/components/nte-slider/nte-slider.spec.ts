import { expect } from 'vitest';
import { NteSliderElement } from './nte-slider';

describe('nte-slider', () => {
  it('should create an element', () => {
    const el = new NteSliderElement();
    expect(el).toBeInstanceOf(NteSliderElement);
  });

  it.todo('write some real tests!');
});
