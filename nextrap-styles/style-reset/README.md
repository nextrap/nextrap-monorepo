# @nextrap/style-reset

A simple stylesheet that resets the styles of all elements to a consistent baseline.

Use it in your components' shadow DOM styles as a baseline.

Native `dialog` elements keep their platform behavior but no longer use the browser's visual surface, text color or
border. Themes and components that use a `dialog` must therefore define its intended background, color and border.
