{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Base schema for Nx Nextrap generators",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the package/library to generate."
        },
        "path": {
            "type": "string",
            "description": "The full path to the library/package to generate."
        }
    },
    "required": ["name", "path"],
    "additionalProperties": false
}
