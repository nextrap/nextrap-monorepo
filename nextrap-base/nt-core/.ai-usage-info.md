# @nextrap/nt-core

## Purpose

Core factory for all `nte-*` web components.

Use this package when creating a Nextrap element that extends Lit and should get the shared Nextrap / TrunkJS browser mixins in a consistent way.

## Construction rule

- all `nte-*` elements should be constructed via `nte-element()`
- in the current codebase this corresponds to the factory exported by `@nextrap/nt-core`, currently `nextrap_element()`
- use `generate_component(options, definition)` when an object-defined, decorator-free component is
  sufficient; it still builds on `nextrap_element()` and returns a real Lit constructor

## Import

```ts
import { nextrap_element } from '@nextrap/nt-core';
```

## Feature source: `@trunkjs/browser-utils`

`@nextrap/nt-core` is mainly a composition layer over `@trunkjs/browser-utils` mixins.

Reference package:
- `/opt/node_modules/@trunkjs/browser-utils/.ai-usage-info.md`

Prefer the helpers and mixins from `@trunkjs/browser-utils` over ad-hoc implementations for:
- logging
- event bindings
- breakpoint handling
- slot visibility
- loader / ready handling
- timing helpers like `sleep`, `waitFor`, `Debouncer`

## Supported base features

```ts
{
  logging?: boolean;
  slotVisibility?: boolean;
  eventBinding?: boolean;
  breakpoints?: boolean;
  setDefaultStyle?: boolean;
}
```

Current defaults:

```ts
{
  logging: true,
  slotVisibility: false,
  eventBinding: false,
  breakpoints: false,
  setDefaultStyle: true,
}
```

`setDefaultStyle` applies `SetDefaultStyleMixin`: if the host `class` attribute does not contain any class starting with `style-`, the mixin automatically adds `style-default`.

## Common examples

### Generated component

```ts
import { generate_component, html } from '@nextrap/nt-core';

export const NteGreeting = generate_component(
  { tagName: 'nte-greeting', slots: ['actions'] },
  {
    $attributes: {
      name: { type: String, initial: 'World' },
    },
    $methods: {
      greet() {
        return `Hello ${this.name}`;
      },
    },
    $template({ $attributes }) {
      return html`<p>${$attributes.name}</p><slot></slot>`;
    },
  },
);

const content = document.createElement('p');
content.textContent = 'Light DOM content';
const action = document.createElement('button');

const greeting = new NteGreeting({ name: 'Nextrap', $slots: content }, { actions: action });
greeting.greet();
```

Generated definitions may contain `$options`, `$attributes`, `$state`, `$fn`, `$methods`,
`$events`, `$lifecycle`, and `$template`. Internal callbacks use method syntax and can call each
other with `this.$call(name, ...args)`. Prefer a typed Lit template callback over parsing HTML
strings with expression placeholders. See `README.md` for the complete API and limitations.

Programmatic Light DOM accepts one `HTMLElement`, an `HTMLElement[]`, or an object/`Map` keyed by
the slot names declared through `slots: ['name', ...]`. The inferred names drive code completion;
strings and other node types are intentionally rejected.

### Minimal `nte-*` element

```ts
import { LitElement, html, unsafeCSS } from 'lit';
import { customElement } from 'lit/decorators.js';
import { nextrap_element } from '@nextrap/nt-core';
import '@nextrap/style-base';
import style from './nte-demo.scss?inline';

@customElement('nte-demo')
export class NteDemo extends nextrap_element() {
  static override styles = [unsafeCSS(style)];

  override render() {
    return html`<slot></slot>`;
  }
}
```

### With slot visibility

```ts
import { html } from 'lit';
import { customElement } from 'lit/decorators.js';
import { nextrap_element } from '@nextrap/nt-core';

const features = {
    logging: true,
    slotVisibility: false,
    eventBinding: false,
    breakpoints: false
};

@customElement('nte-dialog-like')
export class NteDialogLike extends nextrap_element(features) {
  override render() {
    return html`
      <slot name="title"></slot>
      <slot></slot>
    `;
  }
}
```

### With event bindings from `@trunkjs/browser-utils`

Use this instead of registering event listeners manually in `connectedCallback` – the mixin handles automatic cleanup and supports multiple listeners with the same event.

```ts
import { html } from 'lit';
import { customElement } from 'lit/decorators.js';
import { Listen } from '@trunkjs/browser-utils';
import { nextrap_element } from '@nextrap/nt-core';

@customElement('nte-resize-aware')
export class NteResizeAware extends nextrap_element({ eventBinding: true }) {
  override render() {
    return html`<slot></slot>`;
  }

  @Listen('resize', { target: 'window' })
  onResize() {
    this.requestUpdate();
  }
}
```

### With breakpoint support

```ts
import { customElement } from 'lit/decorators.js';
import { nextrap_element } from '@nextrap/nt-core';

@customElement('nte-responsive-box')
export class NteResponsiveBox extends nextrap_element({ breakpoints: true }) {}
```

Use the shared Breakpoint API for structural responsive behavior across Nextrap components instead of adding ad-hoc media queries. Components can configure their breakpoint via `--breakpoint` and react to the reflected mode attribute. This keeps responsive behavior component-local, reusable in nested layouts, and consistent across the ecosystem. For details, see the breakpoint handling documentation in `@trunkjs/browser-utils` and existing `ntl-*` layout components such as `ntl-2col`.
